import React from 'react'; // Import React
import { View, Text } from 'react-native'; // Ensure necessary imports from 'react-native'

const Home = () => { // Define the component with a return type
  return (
    <View>
        <Text>Home POWA</Text>
    </View>
  );
};

export default Home; // Don't forget to export your component