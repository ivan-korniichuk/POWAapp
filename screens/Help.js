import React from 'react';
import {View} from 'react-native';

const Help = ({navigation}) => {
  return (
    <View
      style={{
        height: 50,
        padding: 20,
        backgroundColor: 'blue',
      }}>
    </View>
  );
};

export default Help;